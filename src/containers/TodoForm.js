import React from "react";

import React from "react";

const TodoForm = props => {
  return (
    <form className="todo-form">
      <input type="text" placeholder="todo here" value={this.state.value} />
    </form>
  );
};

export default TodoForm;
